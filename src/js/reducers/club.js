import { attendance } from "../attendance";

const club = (state = attendance.club, action) => {
    let newState = state;
    switch (action.type) {
    }
    return newState;
}

export default club
